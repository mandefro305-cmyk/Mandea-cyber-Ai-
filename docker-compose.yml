version: '3.8'

services:
  assistant:
    build: .
    ports:
      - "8501:8501"
    environment:
      - AGENTROUTER_API_KEY=${AGENTROUTER_API_KEY}
      - AGENTROUTER_BASE_URL=${AGENTROUTER_BASE_URL:-https://agentrouter.ai/v1}
    volumes:
      - .:/app
    restart: unless-stopped
